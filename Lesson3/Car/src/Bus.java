public class Bus extends Transport {
    public Bus(String model, int maxSpeed, int year) {
        super(model, maxSpeed, year);
    }

    @Override
    public String toString() {
        return "Bus{}";
    }
}
